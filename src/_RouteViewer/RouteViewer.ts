import React from "react";

export class RouteViewer extends React.PureComponent {
    render() {
        return (
            React.createElement('div', {},
                React.createElement('div', {},)
            )
        );
    }
}